name: Cron Schedule
on:
  schedule:
    - cron: '1/1 * * * *'
jobs:
  cron-schedule-job:
    runs-on: ubuntu-latest
    steps:
      - name: echo the cron schedule is triggered
        run: echo "cron schedule is triggered"