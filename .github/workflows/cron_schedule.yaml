name: Cron Schedule
on:
  schedule:
    - cron: '* * * * *'
jobs:
  cron-schedule-job:
    runs-on: ubuntu-latest
    steps:
      - name: echo the cron schedule is triggered
        run: echo "cron schedule is triggered"